---
const { content } = Astro.props;
const rotate = Math.random() * 2 - 1;
---

<div class="card-box" style={`rotate: ${rotate}deg;`}>
  <div class="card">
    <div class="title">
      <p>性格</p>
    </div>
    <div class="text">
      <p>{content}</p>
    </div>
  </div>
  <div class="card-shadow"></div>
</div>

<style>
  .card-box.animate {
    animation: slideInIdentity 0.7s ease-out;
  }

  .card-box {
    display: grid;
    place-items: center;
  }

  .card {
    position: relative;
    width: 100%;
    background-color: #C4D4F2;
    background: linear-gradient(260deg, #e8f0ff 65%, #dfe7ff 100%);

    padding: 5% 8%;
  }

  @keyframes slideInIdentity {
    0% {
      opacity: 0;
      transform: translateY(-100%) translateX(-20%);
    }
    100% {
      opacity: 1;
      transform: translateY(0) translateX(0);
    }
    
  }

  .title p {
    font-size: 1.4rem;
    color: #bbb;
    user-select: none;
  }

  .text p {
    font-size: 1.6rem;
    text-align: center;
  }

  .card-shadow {
    position: relative;
    width: 100%;
    height: 1px;
  }

  .card-shadow:before, .card-shadow:after
  {
    z-index: -1;
    position: absolute;
    content: "";
    top: 0px;
    left: 10px;
    width: 40%;
    height: 1px;
    max-width:300px;
    box-shadow: 0 15px 10px #777;
    transform: translateY(-16px) rotate(-3deg);
  }
</style>